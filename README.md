# J CRM - React App

A simple CRM for managing leads generated from Meta ads.

## Features
- View leads dashboard
- Add and edit leads
- Status tracking for nurturing

## Setup
1. Clone the repo.
2. Run `npm install` and `npm start`.

## Deploy
Run `npm run deploy` for GitHub Pages.
